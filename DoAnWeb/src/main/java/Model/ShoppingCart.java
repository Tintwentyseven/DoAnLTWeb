package Model;

import java.util.ArrayList;
import java.util.List;

public class ShoppingCart {
	/*
	 * private List<Cart> CartList;
	 * 
	 * public ShoppingCart() { this.CartList = new ArrayList<>(); }
	 * 
	 * public void add(Cart Cart){ for (Cart i : CartList){
	 * if(i.getProduct().getId()==Cart.getProduct().getId()){ i.addQuantity(1);
	 * return; } } this.CartList.add(Cart); } public void remove(String id){
	 * for(Cart c :CartList){ if(c.getProduct().getId().equals(id)){
	 * CartList.remove(c); return; } } } public void update(String id, int
	 * quanlity){ for(Cart c : CartList){ if(c.getProduct().getId().equals(id)){
	 * c.setQuantity(quanlity); return; } } } public int getSize(){ int re= 0; for
	 * (Cart c:CartList){ re +=c.getQuantity(); } return re; } public List<Cart>
	 * getCartList(){ return this.CartList; }
	 * 
	 * public double getTotalPrice() { double re = 0; for(Cart c: CartList){
	 * re+=c.getTotalPrice(); } return re; }
	 */

}
